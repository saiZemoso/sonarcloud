import createTheme from "@mui/material/styles/createTheme"

export const theme = createTheme({
  palette: {
    mode: 'dark',
  },
  typography: {
    body1: {
      fontStyle:'italic',
    },
  }
});

